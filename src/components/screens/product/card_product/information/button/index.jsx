import React from 'react';

const BattonAddToCart = (props) => {
    return (
        <button type="submit" name="addtocart" defaultValue="5" className="btn amado-btn">
            Add to cart
      </button>
    );
}

export default BattonAddToCart;